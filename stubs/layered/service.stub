<?php

namespace App\Services\{{module}};

use App\DTOs\ExistsDTO;
use App\Repositories\{{module}}\{{name}}Repository;
use App\DTOs\{{module}}\Create{{name}}DTO;
use App\DTOs\{{module}}\Update{{name}}DTO;

class {{name}}Service
{
    protected ${{nameLower}}Repo;

    public function __construct({{name}}Repository ${{nameLower}}Repo)
    {
        $this->{{nameLower}}Repo = ${{nameLower}}Repo;
    }

    public function all()
    {
        return $this->{{nameLower}}Repo->all();
    }

    public function find($id)
    {
        return $this->{{nameLower}}Repo->find($id);
    }

    public function create(Create{{name}}DTO $dto): array
    {
        $success = $this->{{nameLower}}Repo->create($dto->toArray());

        return [
            'success' => $success,
            'message' => $success ? '{{name}} created successfully.' : 'Failed to create {{name}}.',
        ];
    }

    public function update($id, Update{{name}}DTO $dto): array
    {
        return DB::transaction(function () use ($id, $dto) {
            $exists = $this->{{nameLower}}Repo->find($id);

            if (!$exists) {
                return ['success' => false, 'message' => '{{name}} not found.'];
            }

            $affected = $this->{{nameLower}}Repo->update($id, $dto->toArray());

            return [
                'success' => $affected >= 0,
                'message' => '{{name}} updated successfully.',
            ];
        });
    }

    public function checkExist(ExistsDTO $dto): bool
    {
        return $this->{{nameLower}}Repo->checkExists($dto->columns, $dto->values);
    }
}
